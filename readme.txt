this java program was tested in the windows 10 environment only using cmd.
there are various batch files to automatically compile, build or execute the programs.
start seission(host + client).bat is used for a regular start.
in case of mannual startup, make sure host is started first so that the initial packet sent by client is not lost. 